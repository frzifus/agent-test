# Issue #7: Implement the Add function in math.go

## Problem

The `Add` function in `math.go` is currently a stub that returns 0. It should return the sum of its two integer parameters.

```go
func Add(a, b int) int {
    return 0 // TODO: implement
}
```

The task is to implement this function correctly and add unit tests for it.

## Current State

The codebase contains:

1. **math.go** — A single file in the `agent_test` package with the following functions:
   - `Add(a, b int) int` — returns 0 (stub, needs implementation)
   - `Sub(a, b int) int` — returns 0 (stub)
   - `Mul(a, b int) int` — returns 0 (stub)
   - `Div(a, b int) (int, error)` — returns 0, nil (stub)
   - `IsOdd(n int) bool` — implemented, returns `n % 2 != 0`
   - `IsEven(n int) bool` — implemented, returns `n % 2 == 0`

2. **math_test.go** — Test file with tests for:
   - `TestIsOdd` — comprehensive test cases for IsOdd function
   - `TestIsEven` — comprehensive test cases for IsEven function

No tests exist yet for the Add function.

## Proposed Changes

### Files to Modify

1. **math.go** — Implement the Add function:
   - Change `return 0` to `return a + b`

### Files to Create

1. **math_test.go** — Add test function for Add:
   - `TestAdd` — test cases for Add function covering:
     - Basic positive addition
     - Addition with zero
     - Negative number addition
     - Large number addition
     - Mixed positive and negative numbers

### Function Signature

```go
func Add(a, b int) int
```

### Implementation

```go
func Add(a, b int) int {
    return a + b
}
```

## Acceptance Criteria

- [ ] `Add(a, b int) int` function returns the sum of a and b
- [ ] Add correctly handles positive numbers (e.g., 2 + 3 = 5)
- [ ] Add correctly handles zero (e.g., 0 + 5 = 5, 5 + 0 = 5)
- [ ] Add correctly handles negative numbers (e.g., -2 + -3 = -5)
- [ ] Add correctly handles mixed positive and negative (e.g., 5 + -3 = 2)
- [ ] Add correctly handles large numbers
- [ ] Test file math_test.go includes TestAdd function
- [ ] Tests cover edge cases (zero, negatives, large numbers)
- [ ] All tests pass

## Test Cases

### TestAdd

| Input (a, b) | Expected | Description |
|--------------|----------|-------------|
| (2, 3) | 5 | Basic positive addition |
| (0, 0) | 0 | Zero plus zero |
| (0, 5) | 5 | Zero plus positive |
| (5, 0) | 5 | Positive plus zero |
| (-2, -3) | -5 | Negative plus negative |
| (-5, 3) | -2 | Negative plus positive (result negative) |
| (5, -3) | 2 | Positive plus negative (result positive) |
| (999999, 1) | 1000000 | Large numbers |
| (-999999, -1) | -1000000 | Large negative numbers |
| (1000000, -1000000) | 0 | Large numbers cancel out |
